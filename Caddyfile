# Default Caddyfile - Redirects all traffic to https://it-dw.com
{
	email zerossl@it-dw.com
	acme_ca https://acme.zerossl.com/v2/DV90
	admin 0.0.0.0:2019
	log {
		level INFO
		output stdout
	}
}

import sites-enabled/*

# Catch-all redirect for all incoming requests
:80, :443 {
    redir https://it-dw.com temporary
}